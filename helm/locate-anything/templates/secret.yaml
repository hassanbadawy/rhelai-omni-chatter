{{- if .Values.ngcApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "locate-anything.fullname" . }}-ngc
  labels:
    {{- include "locate-anything.labels" . | nindent 4 }}
type: Opaque
stringData:
  NGC_API_KEY: {{ .Values.ngcApiKey | quote }}
{{- end }}
