apiVersion: v1
kind: Service
metadata:
  name: {{ include "locate-anything.fullname" . }}
  labels:
    {{- include "locate-anything.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "locate-anything.selectorLabels" . | nindent 4 }}
